name: "CLA Assistant"
on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened,closed,synchronize]

# explicitly configure permissions, in case your GITHUB_TOKEN workflow permissions are set to read-only in repository settings
permissions:
  actions: write
  contents: write # this can be 'read' if the signatures are in remote repository
  pull-requests: write
  statuses: write

jobs:
  CLAAssistant:
    runs-on: ubuntu-latest
    steps:
      - name: "CLA Assistant"
        if: (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') || github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # the below token should have repo scope and must be manually added by you in the repository's secret
          # This token is required only if you have configured to store the signatures in a remote repository/organization
          # PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        with:
          path-to-signatures: 'signatures/version1/cla.json'
          path-to-document: 'https://github.com/scan-test-org/scan-test/blob/main/.github/workflows/Alibaba_Documentation_Open_Source_CLA.md' # e.g. a CLA or a DCO document
          # branch should not be protected
          branch: 'main'
          allowlist: masquee,Doki12315,JaneLiuL,zhaikangqi331,DamosChen,lokidundun,InJunee,SharlyCheung,oneFiish,is-xuwei,rz-yang,Sunrisea,kaplax,lc0138,zhwaaaaaa,qshuai,EndlessSeeker,dexchong,3533482622,Libres-coder,Patrisam,SaladDay,lys1313013,ShlyxCoder,wetsion,nmgliangwei,LYH-RAIN,zhenghaoran947,jue-yin,learnerjohn,yofine,yangjuanjuan121,buptzxd,panxiaojun233,lixingjia77,co63oc,shuk777,OuterCyrex,Aias00,OxalisCu,KarlManong,zat366,MichealJl,Guo-Chenxu,kai2321,Xscaperrr,erasernoob,zhangjingcn,wxm155,lz19931106,hongzhouzi,HecarimV,JayLi52,NorthernBob,undertaker86001,daixijun,uuuyuqi,Tsukilc,Thomas-Eliot,kenneth-bro,luoxiner,hzhswyz,popsiclexu,NathanChan,Fengxq2014,wilkinszhang,daidaipianzi,jujiale,VinciWu557,xuruidong,josephwww,shubham1172,lhlxc,mirror58229,ishells,hanans426,mrhaoji,guluguluhhhh,lcfang,xingyunyang01,psxjoy,YJQ1101,iendi,DevOpserzhao,007gzs,LofiSu,robynron,xpy01xpy,hanxiantao,Ink-33,Tinie13,Hazel0928,imp2002,lingdie,yldian,Jing-ze,cr7258,robotism,java4candy,alexzzh,yuluo-yx,OnlyPiglet,liuqiufeng,Erica177,flystar32,sjcsjc123,iHx-rainbow,zhangjiahaol,MregXN,jiamiao442,yingjianjian,lexburner,yuchang01,totalo,vikizhao156,ronething,Fkbqf,2456868764,WeixinX,shouao,ruansheng8,rinfx,liangxiong3403,casun18,tomkerkhove,laojun,dspo,zyhui98,CraigMChen,Charlie17Li,jameszhangyukun,wojesen,Xunzhuo,Hinsteny,songpengyuan,Risker-C,spacewander,CH3CHO,xiangxiuhui,slievrly,abuexclusive,kwtboom,heimanba,katepangLiu,innerpeacez,SpecialYang,sjtuzbk,SAMZONG,johnlanni,bot*

         # the followings are the optional inputs - If the optional inputs are not given, then default values will be taken
          #remote-organization-name: enter the remote organization name where the signatures should be stored (Default is storing the signatures in the same repository)
          #remote-repository-name: enter the  remote repository name where the signatures should be stored (Default is storing the signatures in the same repository)
          #create-file-commit-message: 'For example: Creating file for storing CLA Signatures'
          #signed-commit-message: 'For example: $contributorName has signed the CLA in $owner/$repo#$pullRequestNo'
          #custom-notsigned-prcomment: 'pull request comment with Introductory message to ask new contributors to sign'
          #custom-pr-sign-comment: 'The signature to be committed in order to sign the CLA'
          #custom-allsigned-prcomment: 'pull request comment when all contributors has signed, defaults to **CLA Assistant Lite bot** All Contributors have signed the CLA.'
          #lock-pullrequest-aftermerge: false - if you don't want this bot to automatically lock the pull request after merging (default - true)
          #use-dco-flag: true - If you are using DCO instead of CLA
