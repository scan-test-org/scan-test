apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-portal-admin
  labels:
    app: api-portal-admin
spec:
  replicas: {{ .Values.api_portal_admin.replicaCount }}
  selector:
    matchLabels:
      app: api-portal-admin
  template:
    metadata:
      labels:
        app: api-portal-admin
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "product-api-portal.serviceAccountName" . }}
      containers:
        - name: api-portal-admin
          image: "{{ .Values.hub }}/{{ .Values.api_portal_admin.image.repository }}:{{ .Values.api_portal_admin.image.tag}}"
          imagePullPolicy: {{ .Values.api_portal_admin.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.api_portal_admin.serverPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: api-portal-admin
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
