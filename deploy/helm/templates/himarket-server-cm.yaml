kind: ConfigMap
apiVersion: v1
metadata:
  labels:
    app: himarket-server
  name: himarket-server
data:
  # 应用基本配置（非敏感信息）
  SERVER_PORT: "8080"
  # 其他非敏感配置可以在这里添加
  # 所有敏感的数据库配置已移至 himarket-server-secret